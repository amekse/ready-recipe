type TSuggestion = {
    id : number,
    name: string,
    image: string,
    prepTimeMinutes: number,
    cookTimeMinutes: number,
    cuisine: string,
    difficulty: string
}

export type {
    TSuggestion
}